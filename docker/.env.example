MYSQL_ROOT_PASSWORD=cambia-esta-clave-root

DB_NAME=finanzas_db
DB_USER=finanzas_user
DB_PASSWORD=cambia-esta-clave

# Genera con: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=

# Genera con: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FIELD_ENCRYPTION_KEY=

# Genera con: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=

# Tamaño máximo de subida (MB). Aumentar si necesitas restaurar una BD grande (default: 15).
# MAX_CONTENT_UPLOAD_MB=50

# Límite de descompresión del respaldo (MB). Aumentar para BDs > 500 MB (default: 500).
# MAX_RESTORE_SQL_MB=500
